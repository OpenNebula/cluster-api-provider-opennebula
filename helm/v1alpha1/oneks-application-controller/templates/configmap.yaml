apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "oneks-application-controller.name" . }}
  namespace: oneks-system
  labels:
    {{- include "oneks-application-controller.labels" . | nindent 4 }}
data:
  cluster-id: {{ required "clusterID is required" .Values.clusterID | quote }}
