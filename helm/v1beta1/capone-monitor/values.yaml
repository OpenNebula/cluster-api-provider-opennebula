image:
  repository: ghcr.io/opennebula/cluster-api-provider-opennebula-monitor
  # Defaults to the chart appVersion (for example v0.2.0).
  tag: ""
  pullPolicy: IfNotPresent

monitor:
  endpoint: ""
  clusterID: ""
  chartNamespace: kube-system
  chartAnnotation: oneks.opennebula.io/chart-id
  resyncPeriod: 10m
  httpTimeout: 10s
